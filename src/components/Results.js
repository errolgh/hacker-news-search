export default function Results () {
// this component will render results based on the results reducer
// i will need to use useSelector here and possibly a state object to update results

    return (
        <div>
            {false &&
                <div>
                    <h3 className="SubHeader">Results</h3>
                    <button classname= "Button-margin">Clear Results</button>
                </div>
            }
            
        </div>
    )
}