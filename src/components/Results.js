export default function Results () {
    return (
        <div>
            <h3 className="SubHeader">Results</h3>
            <p>Only show me when results.length is greater than 0</p>
            <button>Clear Results</button>
        </div>
    )
}